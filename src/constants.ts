export enum Inputs {
  SECRETS = 'secrets',
  PARSE_JSON = 'parse-json',
  SUPPRESS_POSIX_WARNING = 'suppress-posix-warning',
  ADD_TO_STEPS_ENV = 'add-to-steps-env',
  ADD_TO_STEP_OUTPUT = 'add-to-step-output',
  MASK_SECRETS = 'mask-secrets'
}
